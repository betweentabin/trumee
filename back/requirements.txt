# Core Django packages
Django==5.0.6
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.7.0
django-environ==0.12.0
dj-rest-auth==6.0.0
django-allauth==0.61.1

# Database (SQLite is built-in with Python)
psycopg2-binary==2.9.9  # PostgreSQL for Railway (uncomment for production)
dj-database-url==2.1.0  # Database URL parsing for Railway

# Server
gunicorn==22.0.0
whitenoise==6.6.0  # Static file serving for Railway
daphne==4.1.2  # WebSocket support for Django Channels
channels==4.1.0  # WebSocket support

# Authentication & Security
cryptography==45.0.2
PyJWT==2.10.1
bcrypt==4.3.0

# HTTP & API
requests==2.32.3
urllib3==2.4.0
certifi==2025.1.31

# Utilities
python-dotenv==1.1.0
tzdata==2025.2

# Payment
stripe==12.4.0

# Development & Testing
django-extensions==3.2.3  # Additional Django commands
django-debug-toolbar==4.4.6  # Debug toolbar for development

# Caching (Redis support for future)
redis==5.2.1
django-redis==5.4.0

# Environment management
python-decouple==3.8  # Better environment variable handling

# Additional dependencies for Railway deployment
asgiref==3.8.1
sqlparse==0.5.3
pytz==2025.2

# Firebase support (if needed)
firebase-admin==7.0.0

# Additional utilities
Pillow==11.0.0  # Image processing
reportlab==4.2.5  # PDF generation
# python-magic==0.4.27  # File type detection (uncomment if needed)
